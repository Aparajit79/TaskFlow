{
  "name": "taskflow-root",
  "version": "1.0.0",
  "description": "TaskMatrix Root Scripts",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "server": "npm run dev --prefix backend",
    "server:start": "npm start --prefix backend"
  }
}
